import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        marginHorizontal: 15,
        marginTop: 20,
        marginBottom: 25
    },
    title: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 15,
    }
})