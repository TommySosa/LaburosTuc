import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    image: {
        resizeMode: "contain",
        width: "100%",
        height: 200,
        // marginTop: 15
    },
    content: {
        marginTop: -35,
        marginHorizontal: 40,
    }
})

